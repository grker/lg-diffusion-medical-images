reg_loss:
  loss_fns_config:
    DiceLoss:
      scale: 1.0
      args:
        include_background: False
        sigmoid: False
        reduction: mean
        softmax: True
  log_loss_parts: False
weighting: 0.5
repeated: False
average_ensemble: last
mode_for_reference_mask: segmentation
